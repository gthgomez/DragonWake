import React from "react";
import ReactDOM from "react-dom/client";
import { SpritePreviewApp } from "./SpritePreviewApp";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <SpritePreviewApp />
  </React.StrictMode>
);
