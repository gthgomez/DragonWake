# Alpha R1 asset ledger

## Provider state

Observed 2026-08-31: the local governed AGES installation is
`C:\Workspace\ages-trust-boundary-hardening\tools\gamedev`. Its verified
Phase 5 record marks live AI image generation and live VLM evaluation
`BLOCKED_EXTERNAL`; the provider code does not expose a callable Antigravity
bridge. The private `gthgomez/gamedev` repository exists, but no local clone
or authenticated bridge was found on the implementation path.

`ANTIGRAVITY_AUTH_PATH_USED`: none. No credential files were read, copied, or
created. No separately billed Gemini/OpenAI generation was attempted.

## Candidate and promotion record

| Asset set | Candidates generated | Rejected | Promoted | Status |
|---|---:|---:|---:|---|
| Alpha R1 city/world/dragon art | 0 | 0 | 0 | BLOCKED_EXTERNAL provider |

This campaign therefore contains no generated candidates and no canonical
generated assets claiming AGES promotion. The web implementation keeps the
existing authored vector language and adds the visual contract/asset hooks so
the governed provider can be attached later without a second asset system.

## Required next provider action

Wire the existing Antigravity-authenticated Gemini bridge into AGES provider
discovery, then run the P0 asset set through quarantine, evaluation, governed
promotion, deterministic derivative generation, and rollback verification.
